name: entretien_chaudiere_flutter
description: Application mobile Flutter pour fiches client et rapports d'entretien chaudiere.
publish_to: "none"
version: 0.24.0+24

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  pdf: ^3.12.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
